
server {
	listen 80;
	listen [::]:80;

    # api application
	location / {
		proxy_pass http://localhost:8080;
	}
    
    # web static res
	location ~ ^/(img|js|css|fonts)/ {
        root /home/Relaxion/front/deployment;
	}

    # icon
	location = /favicon.ico {
        root /home/Relaxion/front/deployment;
	}

    # static resource
    location ^ ~ /resource {
        root /home/Relaxion/front/deployment;
	}

    # manager background interface
    location /kris {
        root /home/Relaxion/front;
        try_files $uri $uri/ kris/index.html;
	}

	# log
	access_log /home/ubuntu/Relaxion/logs/log.log;
	error_log /home/ubuntu/Relaxion/logs/error.log;
}
